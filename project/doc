
class math
{
  Object op1;
  Object op2;

  enum type1;
  enum type2;

  enum op;      //+, -, *, /, ...//
}

+, - , *, /, %,
+=, -=, *=, /=, %=,
==, !=, <=, >=, <, >,
~, ^, |, &

class call
{
  int argc;
  string argv[];

  string name;
}

class code_s
{
  code list[]; 
}

class branch
{
  object math;
  object code_s[];

  list mapping; //math result and code array.//
}

