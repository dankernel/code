
class math
{
  Object op1;
  Object op2;

  enum op1_ty;
  enum op2_ty;

  enum op;      //+, -, *, /, ...//
}

+, - , *, /, %,
+=, -=, *=, /=, %=,
==, !=, <=, >=, <, >,
~, ^, |, &

class call
{
  int argc;
  string argv[];

  string name;
}

class branch
{
  object math;
  object code;
}







